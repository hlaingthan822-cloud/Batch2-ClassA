info:
  name: Create Customer
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4000/api/customers
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "name":"su su",
        "email":"susu3@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
